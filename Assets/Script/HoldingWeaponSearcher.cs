using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HoldingWeaponSearcher : MonoBehaviour
{
    ShotPoint shotpoint;
    // Start is called before the first frame update
    void Start()
    {
        //shotpoint = this.transform.Find("FirePoint").GetComponent<ShotPoint>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
